{
  "version": 2,
  "buildCommand": "cd app/web_assessment_react && npm install && npm run build",
  "functions": {
    "api/index.py": {
      "maxDuration": 60
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "api/index.py" }
  ]
}
